import { text } from '../components/typography/text/recipe';
import { kbd } from '../components/typography/kbd/recipe';
import { button } from '../components/button/recipe';
import { spinner } from '../components/spinner/recipe';
import { textField } from '../components/text-field/recipe';
import { combobox } from '../components/combobox/recipe';
import { select } from '../components/select/recipe';
import { skeleton } from '../components/skeleton/recipe';
import { fileUpload } from '../components/file-upload/recipe';
import { collapsible } from '../components/collapsible/recipe';
import { progress } from '../components/progress/recipe';
import { badge } from '../components/badge/recipe';
import { tooltip } from '../components/tooltip/recipe';

export const recipes = {
	text,
	kbd,
	button,
	spinner,
	skeleton,
	badge,
};

export const slotRecipes = {
	textField,
	combobox,
	select,
	fileUpload,
	collapsible,
	progress,
	tooltip,
};
